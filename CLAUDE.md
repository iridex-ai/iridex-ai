# CLAUDE.md
version: 1.0
role: code-surface project layer — iridex-ai (personal-brand profile)
deploy: auto-loaded (Code walks cwd upward)
status: active

Inherits the account-level canon (auto-composed). Adds repo specifics only.

- **Owner:** iridex-ai (iridex dev/brand persona). **Surface:** code. **Visibility:** public.
- **Role:** the `iridex` GitHub profile README repo — public-facing personal-brand home. No
  chat slug; content is presentation, not a code build.
- **Voice:** iridex = applied AI/ML dev persona (principal at CDI). Keep public-facing and
  brand-consistent; no CDI-internal or sensitive detail.
